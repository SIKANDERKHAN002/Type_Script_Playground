/**
 * Prints the text "HelloTS" to the console.
 *
 * This statement uses the built-in console.log() method
 * to display a message in the terminal, browser console,
 * or development environment output window.
 *
 * Output:
 * HelloTS
 */

console.log("HelloTS")